# name: test/sql/airport.test
# description: test airport extension
# group: [airport]

# Before we load the extension, this will fail
statement error
SELECT airport('Sam');
----
Catalog Error: Scalar Function with name airport does not exist!

# Require statement will ensure this test is run with this extension loaded
require airport

# Confirm the extension works
query I
SELECT airport('Sam');
----
airport Sam 🐥

query I
SELECT airport_openssl_version('Michael');
----
<REGEX>:airport Michael, my linked OpenSSL version is OpenSSL.*