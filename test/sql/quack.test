# name: test/sql/arrow_flight.test
# description: test arrow_flight extension
# group: [arrow_flight]

# Before we load the extension, this will fail
statement error
SELECT arrow_flight('Sam');
----
Catalog Error: Scalar Function with name arrow_flight does not exist!

# Require statement will ensure this test is run with this extension loaded
require arrow_flight

# Confirm the extension works
query I
SELECT arrow_flight('Sam');
----
arrow_flight Sam 🐥

query I
SELECT arrow_flight_openssl_version('Michael');
----
<REGEX>:arrow_flight Michael, my linked OpenSSL version is OpenSSL.*